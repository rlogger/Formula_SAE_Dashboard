# Copy this file to .env and fill in the values
# NEVER commit .env to version control

# ── Domain ──────────────────────────────────────────────
# Your server's domain (Caddy auto-provisions HTTPS via Let's Encrypt)
# Use "localhost" for local development (serves HTTP only)
DOMAIN=dashboard.yourteam.com

# ── Admin credentials (used on first startup to create admin user) ──
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_password_123

# ── JWT secret for signing tokens (generate with: openssl rand -hex 32) ──
JWT_SECRET=generate_a_secure_random_string_here

# ── CORS allowed origins (comma-separated) ──
# For production, this should match your DOMAIN
ALLOWED_ORIGINS=https://dashboard.yourteam.com

# ── Frontend API URL (used at build time for Next.js) ──
# For production behind Caddy: leave empty (uses relative /api paths via proxy)
# For local dev without Caddy: http://localhost:8000
NEXT_PUBLIC_API_URL=
