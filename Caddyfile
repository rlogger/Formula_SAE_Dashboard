{$DOMAIN:localhost} {
	# All backend API calls go through /api/* prefix
	# Caddy strips the prefix before forwarding to FastAPI
	handle /api/* {
		uri strip_prefix /api
		reverse_proxy backend:8000
	}

	# Frontend (everything else — pages, static assets)
	handle {
		reverse_proxy frontend:3000
	}
}
